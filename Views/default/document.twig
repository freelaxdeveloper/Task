<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{% block title %}Мои проекты{% endblock %}</title>
        <link rel="stylesheet" href="/Static/default/css/style.css">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
        <link rel="shortcut icon" href="/Static/default/favicon.ico">

        <script type="text/javascript"src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.0/jquery.min.js"></script>

    </head>
    <body>
        <header>
            <div class="header">
                <a href="/" id="logo" title="На главную">TODO</a>
                <span class="right">
                    <a href="/register/">Регистрация</a> / <a href="/authorize/">Авторизация</a>
                    <a href="#" title="Выполненные задания"><i class="fa fa-check"></i></a>
                </span>
            </div>
        </header>
        <div id="container">
            <nav>
                <ul>
                    <li><a href="/view/today/">На сегодня</a></li>
                    <li><a href="/view/week/">На неделю</a></li>
                </ul>
                <ul>
                    {% for project in projects %}
                        <li>
                            <a href="/project/{{project.id}}/"><span class="color_project {{project.color}}"></span> {{project.title}}</a><span id="menu{{project.id}}" data-action="action{{project.id}}"><i class="fa fa-ellipsis-v"></i></span>
                            <ul class="display_none" id="action{{project.id}}">
                                <li><a href="/project/complete_all/{{project.id}}/">Выполнить все</a></li>
                                <li><a href="/project/edit/{{project.id}}/">Изменить</a></li>
                                <li><a href="/project/delete/{{project.id}}/">Удалить</a></li>
                            </ul>
                        </li>
                    {% endfor %}
                </ul>

                <div>
                    <ul id="Project">
                        <li><a href="#">+ Добавить проект</a></li>
                    </ul>
                    <form id="formProject" action="#" method="post">
                        <!-- <span id="typeProject"><input class="typeProject" type="text" name="typeProject" value="" disabled></span> -->
                        <span id="typeProject"></span>
                        <input type="text" name="name_project" value="" placeholder="Название проекта"><br>

                        <button type="button" name="button">Добавить</button>
                        <a href="#" class="cancel">Отменить</a>
                    </form>
                </div>
            </nav>
            <div class="content">
                <div class="messages">
                    {% for error in errors %}
                        <div class="error">
                            {{error}}
                        </div>
                    {% endfor %}
                    {% for message in messages %}
                        <div class="message">
                            {{message}}
                        </div>
                    {% endfor %}
                </div>

                {% block header_title %}
                    <div class="header_title">
                        <h1>{{block('title')}}</h1>
                    </div>
                {% endblock %}

                {% block contnet %}{% endblock %}
            </div>
        </div>
        <footer>
            &copy; by Sanek_OS9<span>@mail.ru</span>
        </footer>
        <script src="/Static/default/js/test.js" charset="utf-8"></script>
    </body>
</html>
